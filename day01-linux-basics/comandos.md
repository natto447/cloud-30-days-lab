# Comandos utilizados – Day 01

## Navegação
```bash
pwd
ls -la
cd /etc

 ## Usuários e permissões
 sudo adduser clouduser
sudo usermod -aG sudo clouduser
groups clouduser

## SSH 
sudo systemctl status ssh
ip a
ssh clouduser@IP_DO_SERVIDOR
 
 ## 
 